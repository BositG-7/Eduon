// import React from "react";

// import { CourseContext } from "../context";
// import { useFilter } from "../hooks/use-filter";
// import { IEntity } from "../types";

// interface CourseProps {
//    children: React.ReactNode;
// }

// const Course = ({ children }: CourseProps) => {
//    const [{ course, isLoading }, setState] = useFilter();

//    const methods = {
//       filter: (course: IEntity.Course | null) => setState(prev => ({ ...prev, course }))
//    };

//    return <CourseContext.Provider value={{ course, isLoading, methods }}>{children}</CourseContext.Provider>;
// };

// export default Course;

export{}